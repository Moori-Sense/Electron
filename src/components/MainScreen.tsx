import '../App.css';

function MainScreen() {
  return (
    <div>
      <h1>MooriSense Dashboard</h1>
      <p>System is running.</p>
      {/* 여기에 앞으로 만들 실제 대시보드 UI가 들어갑니다. */}
    </div>
  );
}

export default MainScreen;